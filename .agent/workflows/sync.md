---
description: Sincronizar todos los cambios con el repositorio Git
---

Este workflow automatiza la adición de archivos, el commit con un mensaje descriptivo y el push al repositorio remoto.

// turbo
1. Ejecutar el comando de sincronización:
   ```powershell
   git add . ; git commit -m "Sincronización automática de cambios" ; git push origin main
   ```
